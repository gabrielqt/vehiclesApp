import { Outlet } from "react-router-dom";
import CustomAppBar from "../components/CustomAppBar";

function Base() {
  return (
    <div>
      <CustomAppBar />
    </div>
  );
}

export default Base;
